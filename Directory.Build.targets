<Project>
  <PropertyGroup>
    <TypeScriptCompileBlocked>true</TypeScriptCompileBlocked>
  </PropertyGroup>

  <Target Name="RunBSMake"
          BeforeTargets="CoreCompile">
    <Exec Command="bsmake build $(MSBuildProjectDirectory)" />
  </Target>
</Project>