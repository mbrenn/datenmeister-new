services:
  datenmeister-webserver:
    build:
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - type: bind
        source: /var/lib/datenmeister
        target: /data
    restart: unless-stopped
