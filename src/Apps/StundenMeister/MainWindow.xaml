<Window x:Class="StundenMeister.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StundenMeister"
        mc:Ignorable="d"
        Title="StundenMeister" Height="450" Width="800" WindowStyle="ToolWindow" Loaded="MainWindow_OnLoaded">
    <Grid>
        <Viewbox x:Name="LoadingText" Stretch="Uniform">
            <TextBlock >Loading</TextBlock>
        </Viewbox>
        <Grid x:Name="LoadedAsset" Visibility="Hidden">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="ActiveTime" Grid.ColumnSpan="2"></TextBlock>
            <Button Grid.Row="1" Grid.Column="0">Start</Button>
            <Button Grid.Row="1" Grid.Column="1">End</Button>
        </Grid>
    </Grid>
</Window>
