namespace DatenMeister.Forms;

/// <summary>
/// Defines in the Local Storage that we are now in the Creation Mode
/// </summary>
public class ExtensionCreationMode
{
    public bool IsActive { get; } = true;
}