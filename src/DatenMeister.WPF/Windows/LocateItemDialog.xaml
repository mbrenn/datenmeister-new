<Window x:Class="DatenMeister.WPF.Windows.LocateItemDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:DatenMeister.WPF.Controls"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen"
        Title="LocateItemDialog" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtTitle" FontSize="24"></TextBlock>
        <controls:LocateElementControl x:Name="LocateElementControl" Grid.Row="1" />
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">    
            <Button Click="Cancel_Click">Cancel</Button>
            <Button Click="Open_Click" x:Name="SelectionButton">Open</Button>
        </StackPanel>
    </Grid>
</Window>
