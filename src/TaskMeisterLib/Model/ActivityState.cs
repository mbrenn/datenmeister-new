namespace TaskMeister.Model
{
    /// <summary>
    /// Stores the possible task states for the task
    /// </summary>
    public enum ActivityState
    {
        New,
        Inwork,
        Hold,
        Lost,
        Closed
    }
}