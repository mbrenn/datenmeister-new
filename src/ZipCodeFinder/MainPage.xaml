<Page
    x:Class="ZipCodeFinder.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ZipCodeFinder"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*"/>
            <ColumnDefinition Width="70*"/>
        </Grid.ColumnDefinitions>
        <Viewbox VerticalAlignment="Top">
            <StackPanel Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Top">
                <TextBlock HorizontalAlignment="Center">Postleitzahl:</TextBlock>
                <TextBox x:Name="txtZipCode" TextChanged="txtZipCode_TextChanged" Width="200"></TextBox>
            </StackPanel>
        </Viewbox>
        <Viewbox Grid.Row="2" Grid.Column="2" VerticalAlignment="Top" x:Name="viewBoxLocation">
            <StackPanel Width="300" HorizontalAlignment="Center" VerticalAlignment="Top">
                <TextBlock x:Name="txtCity">55130 Mainz</TextBlock>
            </StackPanel>
        </Viewbox>
    </Grid>
</Page>
